% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main_functions.R
\name{tree_graph_analysis}
\alias{tree_graph_analysis}
\title{Tree Graph Angle Analysis}
\usage{
tree_graph_analysis(
  data = NULL,
  start = c(0, 0),
  coverage = 0.95,
  angle_method = c("mean", "min", "median", "equal", "neigh"),
  simulate = FALSE,
  rho1 = 0.5,
  rho2 = 0.7,
  T = 6,
  N = 2,
  Tij = 1,
  n1 = 10,
  n2 = 100
)
}
\arguments{
\item{data}{A matrix or data frame with two columns representing coordinates.
If NULL, the function will generate simulated data.}

\item{start}{Numeric vector of length 2 specifying the starting point coordinates.
Default is c(0, 0).}

\item{coverage}{Numeric value between 0 and 1 specifying the sample coverage level.
Default is 0.95 (95% coverage).}

\item{angle_method}{Character string specifying the angle calculation method.
Options are: "mean", "min", "median", "equal", "neigh". Default is "mean".}

\item{simulate}{Logical indicating whether to run simulation. If TRUE, 
simulation parameters should be provided.}

\item{rho1}{First correlation coefficient for simulation. Default is 0.5.}

\item{rho2}{Second correlation coefficient for simulation. Default is 0.7.}

\item{T}{Depth parameter for simulation. Default is 6.}

\item{N}{Branching factor for simulation. Default is 2.}

\item{Tij}{Time parameter for simulation. Default is 1.}

\item{n1}{Outer repetitions for simulation. Default is 10.}

\item{n2}{Inner repetitions for simulation. Default is 100.}
}
\value{
If analyzing data: the selected angle statistic.
  If running simulation: comparison results between rho1 and rho2.
}
\description{
Main function for analyzing angles in tree-structured graphs with customizable
coverage levels and angle calculation methods.
}
\examples{
# Example 1: Analyze sample data
set.seed(123)
sample_data <- matrix(rnorm(200), ncol = 2)
result <- tree_graph_analysis(data = sample_data, 
                             coverage = 0.95,
                             angle_method = "mean")

# Example 2: Run simulation
sim_result <- tree_graph_analysis(simulate = TRUE,
                                 rho1 = 0.3, 
                                 rho2 = 0.7,
                                 coverage = 0.9,
                                 angle_method = "median")

}
